# 操作手册

写给接手这个仓库的 AI。用户会丢来一道题、一张讲义照片或一个知识点名，你要把它变成
「一句口诀 + 一页讲解 + 一个能点着看的动画」，然后发布出去。

## 读者只有一个人

一个三年级的孩子。所有判断都以他为准：

- 他卡住，**通常不是不会算，是脑子里没有画面**。所以先给画面，再给算式。
- 他读不懂「设未知数」「等量关系」这类词。能用「倒过去」「多出来的那一截」就别用术语。
- 他会把口诀背下来当咒语念。所以口诀必须**描述一个动作**，不能是公式的中文翻译。

判断一页写得好不好，只有一个标准：**合上页面，他能自己把这道题讲给别人听吗。**

## 加一课，按这个顺序走

### 1. 先弄清楚卡点在哪

别急着写。先自己把题做一遍，然后回答：**一个三年级孩子会在哪一步想错，他会怎么想错。**

这个答案决定了后面所有东西——口诀对着它写，动画对着它设计，「容易错在哪」第一条就是它。
想不出卡点，说明这道题不值得单独成课，并进已有的知识点当例题就行。

如果给的是 PDF 讲义且是扫描件（读出来是空白），先转成图片再看：

```
npm run pdf -- ~/Downloads/讲义.pdf
```

### 2. 写知识点页

从 `模板/知识点模板.md` 复制到 `docs/知识点/<知识点名>.md`。模板里每一节都写了填写要求，照着填。

口诀这一条格外重要，它会被首页卡片抓去当摘要，必须能独立看懂：

- 好：「倒过去 1 份，差距缩小 2 份。」「两个算式摆一起，划掉一样多的。」
- 坏：「本节介绍移多补少的基本原理。」「a − b = 2c。」

**知识点已经有了就别新建。** 先看 `docs/知识点/` 下现成的，能挂上去就挂上去，只补一条配套例题链接。

### 3. 写例题页

从 `模板/例题模板.md` 复制到 `docs/题目/<例题名>.md`。

例题页里指向动画的链接要用 **GitHub Pages 的绝对地址**
（`https://qiaojiaozhao.github.io/grade3-math/demos/<文件名>`），不能用 `../demos/`。
`docs/` 是 GitBook 的根，相对路径出不去，而且 GitBook 上也跑不了本地 HTML。

「孩子容易错在哪」至少写三条，第一条就是第 1 步找到的那个卡点。这一节的价值常常比解题过程还高。

### 4. 做动画

从 `模板/动画模板.html` 复制到 `demos/<例题名>-<知识点>.html`，把两处 `../demos/` 改成 `lib/`。
引擎用法见下面「动画怎么写」。

不是每道题都需要动画。**动画的唯一职责是消掉第 1 步那个卡点**，如果讲解里一张 ASCII 图就够了，
就别做动画——做了也是负担。

### 5. 质检

```
npm run shoot -- demos/<你的文件>.html
```

逐幕截图，自动报三类问题：页面超出画幅、旁白气泡压住标签、JS 报错。
**必须全绿再往下走**，而且要把截图真的看一遍——工具只能查排版，查不出「这一幕没讲清楚」。

### 6. 录视频

```
npm run record -- demos/<你的文件>.html
```

mp4 会生成在 html 旁边，方便直接发微信或投屏。改过动画就重录，别让视频和网页对不上。

### 7. 更新目录

```
npm run index    # 重新生成 GitHub Pages 落地页
npm run check    # 查所有内部链接
```

再手工把新页面加进 `docs/SUMMARY.md`（GitBook 的目录）和 `docs/README.md` 里的知识点表格。
这两个文件是手写的，`npm run index` 不会动它们。

### 8. 提交发布

```
git add -A && git commit -m "加一课：<知识点名>" && git push
```

推上去两边都会自动更新：GitHub Pages 跑动画，GitBook 靠 Git Sync 拉 `docs/`。
**仓库是唯一的源**，别在 GitBook 网页上直接改内容，那会和仓库打架。

## 动画怎么写

引擎在 `demos/lib/`，三个文件：

| 文件 | 管什么 |
| --- | --- |
| `anim.css` | 页面外壳、旁白气泡、播放控件。不用改 |
| `anim.js` | 分幕调度、补间、旁白。不用改 |
| `bars.css` / `bars.js` | 条形对比组件。和差倍、平均数、盈亏这类题直接用 |

页面里只写这道题特有的元素和分幕脚本：

```js
const { sleep, cancelled, tween, lerp, say, pop } = Anim;

Anim.run([
  {
    title: '原来的样子',
    async play(id) {
      say('原来', '先<b>不看数字</b>，只看高矮。');
      await sleep(700);
      if (cancelled(id)) return;
    }
  },
], { reset: resetVisuals });
```

`Anim` 提供：`sleep` `cancelled` `tween(ms,id,onFrame)` `lerp` `say(标签,html)` `pop(文字)`
`goto(i)` `playAll()` `isPlaying()` `sceneCount()`。

`Bars` 提供：`set` `tween` `showNumbers` `guide` `split` `diff` `reset`，用法见 `模板/动画模板.html`。

### 五条硬规矩

1. **每个 `await` 后面都要 `if (cancelled(id)) return;`。** 漏一个，用户切走后上一幕的动画会和新一幕打架，
   画面会乱跳。这是这套引擎最容易犯的错。
2. **一幕只讲一个想法。** 讲不完就拆两幕。七幕左右最合适，超过十幕孩子会走神。
3. **先不给数字。** 开头让他看高矮、看多少，`showNumbers(true)` 留到推理出结果那一刻再开。
   数字一上来，他就开始套公式，不看画面了。
4. **结尾定格在答案上。** 最后一幕必须回到题目问的那个状态，配一句写明答案的旁白。
   停在中间某个验算状态，孩子会记错。
5. **清场交给引擎。** 上一幕的残留在 `reset` 里统一清，别在每幕开头重复写。

### 尺寸

画幅按 1280×800 设计，舞台 `--stage-h` 默认 500px。条形高度用「最大的量 × scale ≤ 290」定，
再高就会顶到旁白气泡。`npm run shoot` 会把这类问题报出来。

## 目录

```
docs/            GitBook 站点的内容，也只有这个目录会同步到 GitBook
  README.md      站点首页，写给家长和孩子
  SUMMARY.md     站点目录，手写
  知识点/         一个知识点一页
  题目/           一道例题一页
demos/           互动动画 + 配套 mp4
  lib/           动画引擎，改这里会影响所有动画
模板/            新建页面从这里复制
tools/           命令脚本
index.html       GitHub Pages 落地页，由 npm run index 生成，别手改
```

根目录的 `README.md` 和这份 `AGENTS.md` 是写给维护者的，**不在 `docs/` 里，不会进站点**。
内容页一律放 `docs/` 下。

## 命令

| 命令 | 做什么 |
| --- | --- |
| `npm run setup` | 装依赖和 Chromium，新机器跑一次 |
| `npm run shoot -- <html>` | 逐幕截图质检动画 |
| `npm run record -- <html>` | 录成 mp4 |
| `npm run index` | 重新生成首页 |
| `npm run check` | 查所有内部链接 |
| `npm run pdf -- <pdf>` | 扫描讲义转图片 |

录制依赖 `ffmpeg`，没有就 `brew install ffmpeg`。

## 改引擎的时候

`demos/lib/` 是所有动画共用的。改完必须把**每个**动画都跑一遍 `npm run shoot`，确认没有回归。
