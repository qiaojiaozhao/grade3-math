# 操作手册

写给接手这个仓库的 AI。用户会丢来一道题、一张讲义照片或一个知识点名，你要把它变成
「一句口诀 + 一页讲解 + 一个能点着看的动画」，然后发布出去。

## 读者只有一个人

一个三年级的孩子。所有判断都以他为准：

- 他卡住，**通常不是不会算，是脑子里没有画面**。所以先给画面，再给算式。
- 他读不懂「设未知数」「等量关系」这类词。能用「倒过去」「多出来的那一截」就别用术语。
- 他会把口诀背下来当咒语念。所以口诀必须**描述一个动作**，不能是公式的中文翻译。

判断一页写得好不好，只有一个标准：**合上页面，他能自己把这道题讲给别人听吗。**

## 加一课，按这个顺序走

### 1. 先弄清楚卡点在哪

别急着写。先自己把题做一遍，然后回答：**一个三年级孩子会在哪一步想错，他会怎么想错。**

这个答案决定了后面所有东西——口诀对着它写，动画对着它设计，「容易错在哪」第一条就是它。
想不出卡点，说明这道题不值得单独成课，并进已有的**母题**当「换一身衣服」就行。

如果给的是 PDF 讲义且是扫描件（读出来是空白），先转成图片再看：

```
npm run pdf -- ~/Downloads/讲义.pdf
```

### 2. 写母题页

这个站按**浅奥母题**组织，不按教材章节。先问：这是新骨头，还是旧骨头换了衣服？

- 旧骨头：打开对应母题页，在「换一身衣服」里加一题，例题页末尾也加一条。别新建母题。
- 新骨头：一条命令把三个文件从模板建好，名字和互相之间的链接已填上：

  ```
  npm run new -- <母题名> <例题名>
  ```

  它会生成 `docs/知识点/<母题名>.md`、`docs/题目/<例题名>.md`、`demos/<例题名>-<母题名>.html`，
  已存在的不覆盖（母题已存在就是旧骨头，它会提醒你）。然后再挂进 `docs/SUMMARY.md`、
  `docs/认出母题.md` 和 `docs/大纲.md`（把那一行从「建议下一层」挪到「已经写成课的」）。

口诀这一条格外重要，它会被首页卡片抓去当摘要，必须能独立看懂：

- 好：「倒过去 1 份，差距缩小 2 份。」「两个算式摆一起，划掉一样多的。」
- 坏：「本节介绍移多补少的基本原理。」「a − b = 2c。」

### 3. 写例题页

`npm run new` 已经建好 `docs/题目/<例题名>.md`，把里面的【方括号】填掉。

例题页里指向动画的链接要用 **GitHub Pages 的绝对地址**
（`https://qiaojiaozhao.github.io/grade3-math/demos/<文件名>`），不能用 `../demos/`。
`docs/` 是 GitBook 的根，相对路径出不去，而且 GitBook 上也跑不了本地 HTML。

「孩子容易错在哪」至少写三条，第一条就是第 1 步找到的那个卡点。这一节的价值常常比解题过程还高。

### 4. 做动画

`npm run new` 已经建好 `demos/<例题名>-<母题名>.html`（`lib/` 路径已改好）。引擎用法见下面「动画怎么写」。

不是每道题都需要动画。**动画的唯一职责是消掉第 1 步那个卡点**，如果讲解里一张 ASCII 图就够了，
就别做动画——做了也是负担。

### 5. 质检 + 录视频 + 更新目录：一条命令

```
npm run ship -- demos/<你的文件>.html
```

它把下面四步串起来，质检和录制并行跑，20 秒左右出结果：

- `shoot` 逐幕截图，自动报三类问题：页面超出画幅、旁白气泡压住标签、JS 报错。
  截图落在 `.shots/<动画名>/`，**先看 `sheet.png` 总览图**，一张就能看完所有幕。
- `record` 录成 mp4，放在 html 旁边，1080×1920 竖版。改过动画就重录，别让视频和网页对不上。
- `index` 重新生成 GitHub Pages 落地页。
- `check` 查所有内部链接。

任何一步失败都会非零退出，**必须全绿再往下走**，而且要把 `sheet.png` 真的看一遍——
工具只能查排版，查不出「这一幕没讲清楚」。只想质检不录视频加 `--no-record`；
四步也都能单独跑：`npm run shoot / record / index / check`。

### 6. 目录里的手写部分

落地页是母题地图。新母题还要在 `tools/build-index.mjs` 的 `MOTIFS` 里加一行（名字、GitBook 路径、配色、挂哪段动画），再跑 `npm run index`。

再手工把新页面加进这三个手写文件（`npm run index` 不会动它们）：

- `docs/SUMMARY.md` —— GitBook 侧栏，按母题分组
- `docs/README.md` —— 母题地图上的卡片
- `docs/认出母题.md` —— 「题目给了什么 → 哪道母题」那张表
- `docs/大纲.md` —— 三年级浅奥总目，已有的和还没写的

### 7. 提交发布

做完就推，不用再问。

```
git add -A && git commit -m "加一课：<知识点名>" && git push
```

推上去两边都会自动更新：GitHub Pages 跑动画，GitBook 靠 Git Sync 拉 `docs/`。
**仓库是唯一的源**，别在 GitBook 网页上直接改内容，那会和仓库打架。

## 动画怎么写

引擎在 `demos/lib/`，三个文件：

| 文件 | 管什么 |
| --- | --- |
| `anim.css` | 页面外壳、旁白气泡、播放控件。不用改 |
| `anim.js` | 分幕调度、补间、旁白。不用改 |
| `bars.css` / `bars.js` | 条形对比组件。和差倍、平均数、盈亏这类题直接用 |

页面里只写这道题特有的元素和分幕脚本：

```js
const { sleep, cancelled, tween, lerp, say, pop } = Anim;

Anim.run([
  {
    title: '原来的样子',
    async play(id) {
      say('原来', '先<b>不看数字</b>，只看高矮。');
      await sleep(700);
      if (cancelled(id)) return;
    }
  },
], { reset: resetVisuals });
```

`Anim` 提供：`sleep` `cancelled` `tween(ms,id,onFrame)` `lerp` `say(标签,html)` `pop(文字)`
`goto(i)` `playAll()` `isPlaying()` `sceneCount()`。

`Bars` 提供：`set` `tween` `showNumbers` `guide` `split` `diff` `reset`，用法见 `模板/动画模板.html`。

### 五条硬规矩

1. **每个 `await` 后面都要 `if (cancelled(id)) return;`。** 漏一个，用户切走后上一幕的动画会和新一幕打架，
   画面会乱跳。这是这套引擎最容易犯的错。
2. **一幕只讲一个想法。** 讲不完就拆两幕。七幕左右最合适，超过十幕孩子会走神。
3. **先不给数字。** 开头让他看高矮、看多少，`showNumbers(true)` 留到推理出结果那一刻再开。
   数字一上来，他就开始套公式，不看画面了。
4. **结尾定格在答案上。** 最后一幕必须回到题目问的那个状态，配一句写明答案的旁白。
   停在中间某个验算状态，孩子会记错。
5. **清场交给引擎。** 上一幕的残留在 `reset` 里统一清，别在每幕开头重复写。

### 角色用图片，但**数量必须是代码画的**

条形、色块这类抽象图形直接用 CSS 画。但如果这道题里有具体的东西——小鸡、小兔、苹果、硬币——
用生成的插图会比 CSS 椭圆生动得多。孩子看「8 个椭圆」和看「8 只小鸡」，注意力完全不是一回事。

做法是**身体用图片，要数的东西用 CSS**：

```
demos/assets/鸡.png     身体、脑袋、表情 —— 只管好看
.leg（CSS 元素）        腿 —— 要一根根数清楚，绝不能交给图片
```

`demos/鸡兔同笼-假设法.html` 是现成例子。这条分工不是洁癖：这个站的全部价值就是数字精确，
「兔子有几条腿」要是画在图里，就再也验不了了。凡是题目里要数、要算、要对答案的量，
一律留在 CSS/JS 里。

生成的图都是白底大图，先过一道：

```
npm run cutout -- ~/生成的图.png demos/assets/鸡.png
```

它会抠掉白底、裁到内容边界、缩到 320px 以内。裁边很关键——裁完用
`background-position: bottom center` 就能让高矮不同的角色踩在同一条地平线上。

不同角色在图里的**身体占比可能差很多**（兔子图里两边是耳朵和尾巴，身体只占宽度七成，
鸡则占满），同样的框缩放出来身体会一大一小。按截图给偏小的那个单独放宽框补回来。

### 让画面活起来

三个几乎不花成本但很提神的动效，新动画可以照抄 `鸡兔同笼-假设法.html`：

- **待机微动**：给角色一个两三秒周期的呼吸起伏，每只错开相位（`animationDelay`）。
  完全静止的画面最显死板，加这一条的性价比最高。
- **挤压回弹**：变身、落地这种关键动作，让元素先压扁再弹回（squash & stretch），
  动作立刻有了分量。
- **数字跳动**：数值变化时让数字弹一下，把「只涨了 2」这种关键变化顶到眼前。

### 尺寸

新动画默认黑底：`<body class="theme-dark">`。视频按微信视频号竖版 **1080×1920（9:16）** 录，
`npm run shoot` / `npm run record` 都是这个画幅。上下留安全区，关键数字别贴顶或贴底。
条形高度用「最大的量 × scale」能进舞台、不顶旁白。`npm run shoot` 会报出来。

## 目录

```
docs/            GitBook 站点的内容，也只有这个目录会同步到 GitBook
  README.md      站点首页，写给家长和孩子
  SUMMARY.md     站点目录，手写
  认出母题.md     认题：题目给了什么 → 哪道母题
  大纲.md         三年级浅奥总目，含还没写成课的
  知识点/         一道母题一页
  题目/           一道例题一页
demos/           互动动画 + 配套 mp4
  lib/           动画引擎，改这里会影响所有动画
  assets/        角色插图，用 npm run cutout 处理过的透明底 png
模板/            新建页面从这里复制
tools/           命令脚本
index.html       GitHub Pages 落地页，由 npm run index 生成，别手改
```

根目录的 `README.md` 和这份 `AGENTS.md` 是写给维护者的，**不在 `docs/` 里，不会进站点**。
内容页一律放 `docs/` 下。

## 命令

| 命令 | 做什么 |
| --- | --- |
| `npm run setup` | 装依赖和 Chromium，新机器跑一次 |
| `npm run new -- <母题> <例题>` | 从模板建好母题页、例题页、动画页 |
| `npm run ship -- <html> [--no-record]` | 质检 + 录制并行，再重建首页、查链接 |
| `npm run shoot -- <html>` | 逐幕截图质检动画，并出 `sheet.png` 总览图 |
| `npm run record -- <html>` | 录成 mp4 |
| `npm run index` | 重新生成首页 |
| `npm run check` | 查所有内部链接 |
| `npm run pdf -- <pdf>` | 扫描讲义转图片 |
| `npm run cutout -- <png> [输出]` | 角色图去白底、裁边、缩小 |

录制依赖 `ffmpeg`，没有就 `brew install ffmpeg`。

## 改引擎的时候

`demos/lib/` 是所有动画共用的。改完必须把**每个**动画都跑一遍 `npm run shoot`，确认没有回归。
六个动画可以并行跑（每个各开一个 Chromium），四十多秒全部跑完：

```
for f in demos/*.html; do node tools/shoot.mjs "$f" > "/tmp/shoot-$(basename "$f" .html).log" 2>&1 & done; wait
tail -n 2 /tmp/shoot-*.log
```
